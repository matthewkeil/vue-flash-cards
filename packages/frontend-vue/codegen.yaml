schema: ../backend/dist/src/graphql/schema.graphql
documents: src/**/*.{graphql,gql}
generates:
  ./src/graphql.ts:
    plugins:
      - typescript
      - typescript-operations
      - typed-document-node
